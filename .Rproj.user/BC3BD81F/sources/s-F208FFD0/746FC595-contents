We analayse US gun murders data collected by the FBI.

dowanload-data.R downloads csv file to data directory

wrangle-data.R creates a derived dataset and saves as R object in rds directory

analysis.R generates a plot and saves it to the figs directory